spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store_api?createDatabaseIfNotExist=true
    username: root
    password: Ulan1234
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 7200 #2hrs
    refreshTokenExpiration: 604800 #7days
stripe:
  secreteKey: ${STRIPE_SECRETE_KEY}